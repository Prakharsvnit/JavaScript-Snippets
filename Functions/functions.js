// If the function does not have a return statement, the default return value is undefined.
// If the function contains an error, the return value is undefined.
// If the function is called with invalid arguments, the return value is undefined.
// If the function is called with a value that is not defined, the return value is undefined.


function add(a,b){
    return a - b;
}

console.log(add(2,3));   //5